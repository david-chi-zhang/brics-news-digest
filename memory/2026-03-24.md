# 2026-03-24 工作记录

## 🤖 Agent 配置（重要！）

### 已创建的 Agent

| Agent ID | Workspace 路径 | 专属技能 | 用途 |
|----------|---------------|---------|------|
| `main` | `~/openclaw/workspace` | 通用技能 | 日常默认使用 |
| `imf-weo` | `~/openclaw/workspaces/imf-weo` | `imf-weo-analyzer` | IMF WEO 数据分析 |
| `macro-archiver` | `~/openclaw/workspaces/macro-archiver` | `ima-macro-archiver`, `macro-archiver` | 宏观经济数据归档 |

### 切换命令

**切换到 imf-weo：**
```bash
openclaw agents bind --agent imf-weo --bind openim
```

**切换到 macro-archiver：**
```bash
openclaw agents bind --agent macro-archiver --bind openim
```

**切换回 main（默认）：**
```bash
openclaw agents bind --agent main --bind openim
```

### 查看 Agent 状态
```bash
openclaw agents list
```

---

## 任务记录

### Workspace 隔离项目 (23:19 完成) ✅
- 创建 `imf-weo` workspace
- 创建 `macro-archiver` workspace
- 迁移技能到对应 workspace
- 删除原位置副本
- 创建对应的 agent

---

## 注意事项
- 当前对话绑定在 `main` agent
- 需要切换时，用户会说"切换到 xxx workspace"
- 切换后，该 channel 的所有消息会路由到对应 agent

---

## 通用技能

- `bdpan-storage` - 百度网盘管理（全局技能，所有 agent 可用）
  - 位置：`~/.openclaw/skills/bdpan-storage/`

---

## 已删除技能

- `bdpan-storage` - 已彻底删除 (23:27)
